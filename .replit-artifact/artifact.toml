kind = "web"
previewPath = "/"
title = "El Escudo de un Millón de Dólares"
version = "1.0.0"
id = "artifacts/sales-funnel"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 21750

[services.development]
run = "pnpm --filter @workspace/sales-funnel run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/sales-funnel", "run", "build" ]
publicDir = "artifacts/sales-funnel/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "21750"
BASE_PATH = "/"
